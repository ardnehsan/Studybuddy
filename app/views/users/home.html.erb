<%= render "layouts/header" %>
<div class="btn-group btn-group-justified" role="group" aria-label="...">
    <a href="#" class="btn btn-default">My Classes</a>
    <a href="#" class="btn btn-default">My Achievements</a>
    <a href="#" class="btn btn-default">Settings</a>
</div>

    <div class="quote-container">
        <i class="pin"></i>
        <blockquote class="note yellow">
          <%= image_tag attachment_url(@current_user, :photo, :fill, 300, 300, format: "jpg"), class:"img-circle"%>
            <br>
          <cite class="author">
            <%= @current_user.username %>
            <br>
            <%= @current_user.email %>
          </cite>
        </blockquote>
    </div>
